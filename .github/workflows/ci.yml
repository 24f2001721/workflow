name: CI - 24f2001721@ds.study.iitm.ac.in

on:
  push:
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Print student email for verification
        run: echo "Student email: 24f2001721@ds.study.iitm.ac.in"

      - name: Say hello
        run: echo "Workflow is working perfectly!"
